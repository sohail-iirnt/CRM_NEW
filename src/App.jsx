import { Navigate, NavLink, Route, Routes, useNavigate } from 'react-router-dom'
import { MODULES, ROLES } from './config.js'
import { useAuth } from './auth.jsx'
import Login from './pages/Login.jsx'
import CRM from './pages/CRM.jsx'
import { Warehouse, PDI, Logistics } from './pages/Operations.jsx'
import Admin from './pages/Admin.jsx'
import Analytics from './pages/Analytics.jsx'
import './ui-upgrades.css'
import './responsive-upgrades.css'
const modules=[[MODULES.CRM,'CRM','Tickets','▦'],[MODULES.WAREHOUSE,'Warehousing','Operations','▤'],[MODULES.PDI,'PDI Inspection','Quality Control','◈'],[MODULES.LOGISTICS,'Logistics','Transport','➜']]
function Protected({requiredRole,children}){const{user,profile,loading}=useAuth();if(loading)return <div className="loading-screen">Loading CRM SAIDHARA…</div>;if(!user)return <Navigate to="/login" replace/>;if(!profile)return <div className="loading-screen"><div className="error-box"><strong>Account profile not found.</strong><span>Your Firebase login succeeded, but no active user profile was found for this account. Please contact an administrator.</span></div></div>;if(profile.blocked===true)return <div className="loading-screen"><div className="error-box"><strong>Your account is blocked.</strong><span>Contact an administrator to restore access.</span></div></div>;if(profile.active===false)return <div className="loading-screen"><div className="error-box"><strong>Your account is inactive.</strong><span>Contact an administrator to activate your account.</span></div></div>;if(profile.role!==ROLES.ADMIN&&profile.role!==requiredRole)return <Navigate to={`/${profile.role||ROLES.CRM}`} replace/>;return children}
const sub=(to,n,label)=><NavLink to={to} end className={({isActive})=>isActive?'nav-subitem active':'nav-subitem'}><span className="sub-num">{n}</span><span>{label}</span></NavLink>
function SidebarGroup({keyName,label,subLabel,icon}){return <div className="nav-group"><NavLink to={`/${keyName}`} className={({isActive})=>isActive?'nav-module active':'nav-module'}><span className="nav-icon">{icon}</span><span className="nav-copy"><b>{label}</b><small>{subLabel}</small></span><span className="nav-chevron">›</span></NavLink><div className="nav-subpages">{keyName===MODULES.CRM&&<>{sub('/crm','01','Ticket Entry')}{sub('/crm/log','02','Master Ticket Log')}{sub('/crm/history','03','History & Audit')}</>}{keyName===MODULES.WAREHOUSE&&<>{sub('/warehouse','01','Movement Queue')}{sub('/warehouse/dispatch','02','Dispatch Queue')}{sub('/warehouse/history','03','History & Audit')}</>}{keyName===MODULES.PDI&&<>{sub('/pdi','01','Inspection Queue')}{sub('/pdi/history','02','History & Audit')}</>}{keyName===MODULES.LOGISTICS&&<>{sub('/logistics','01','Pending / ETA')}{sub('/logistics/transit','02','In Transit')}{sub('/logistics/closed','03','Closed Tickets')}{sub('/logistics/history','04','History & Audit')}</>}</div></div>}
function Shell(){const{profile,logout,hasRole}=useAuth();const navigate=useNavigate();const visibleModules=modules.filter(([key])=>hasRole(key));const isAdmin=profile?.role===ROLES.ADMIN;return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">C</span><div><strong>CRM SAIDHARA</strong><small>Operations Platform</small></div></div><div className="nav-label">WORKSPACE</div><nav>{visibleModules.map(([key,label,subLabel,icon])=><SidebarGroup key={key} keyName={key} label={label} subLabel={subLabel} icon={icon}/>)}{isAdmin&&<><div className="nav-divider"/><div className="nav-label">CONTROL CENTER</div><NavLink to="/analytics" className={({isActive})=>isActive?'nav-module standalone active':'nav-module standalone'}><span className="nav-icon">◫</span><span className="nav-copy"><b>Analytics</b><small>Operations Intelligence</small></span></NavLink><NavLink to="/admin" className={({isActive})=>isActive?'nav-module standalone active':'nav-module standalone'}><span className="nav-icon">⚙</span><span className="nav-copy"><b>Admin Control Center</b><small>System Administration</small></span></NavLink></>}</nav><div className="role-card"><div className="user-avatar">{(profile?.name||profile?.email||'U').slice(0,1).toUpperCase()}</div><div className="user-copy"><small>Signed in as</small><strong>{profile?.name||profile?.email}</strong><span>{profile?.role?.toUpperCase()}</span></div><button className="signout" onClick={async()=>{await logout();navigate('/login')}} title="Sign out">↪</button></div></aside><main className="main"><header className="topbar"><div><span className="status-dot"/>Live Operations</div><span>{profile?.name||profile?.email}</span></header><Routes><Route path="/crm" element={<Protected requiredRole={ROLES.CRM}><CRM/></Protected>}/><Route path="/crm/log" element={<Protected requiredRole={ROLES.CRM}><CRM view="log"/></Protected>}/><Route path="/crm/history" element={<Protected requiredRole={ROLES.CRM}><CRM historyOnly/></Protected>}/><Route path="/warehouse" element={<Protected requiredRole={ROLES.WAREHOUSE}><Warehouse view="movement"/></Protected>}/><Route path="/warehouse/dispatch" element={<Protected requiredRole={ROLES.WAREHOUSE}><Warehouse view="dispatch"/></Protected>}/><Route path="/warehouse/history" element={<Protected requiredRole={ROLES.WAREHOUSE}><Warehouse historyOnly/></Protected>}/><Route path="/pdi" element={<Protected requiredRole={ROLES.PDI}><PDI/></Protected>}/><Route path="/pdi/history" element={<Protected requiredRole={ROLES.PDI}><PDI historyOnly/></Protected>}/><Route path="/logistics" element={<Protected requiredRole={ROLES.LOGISTICS}><Logistics view="pending"/></Protected>}/><Route path="/logistics/transit" element={<Protected requiredRole={ROLES.LOGISTICS}><Logistics view="transit"/></Protected>}/><Route path="/logistics/closed" element={<Protected requiredRole={ROLES.LOGISTICS}><Logistics view="closed"/></Protected>}/><Route path="/logistics/history" element={<Protected requiredRole={ROLES.LOGISTICS}><Logistics historyOnly/></Protected>}/><Route path="/analytics" element={<Protected requiredRole={ROLES.ADMIN}><Analytics/></Protected>}/><Route path="/admin" element={<Protected requiredRole={ROLES.ADMIN}><Admin/></Protected>}/><Route path="/" element={<Navigate to={profile?.role===ROLES.ADMIN?'/admin':`/${profile?.role||ROLES.CRM}`} replace/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main></div>}
export default function App(){const{user}=useAuth();if(!user)return <Routes><Route path="/login" element={<Login/>}/><Route path="*" element={<Navigate to="/login" replace/>}/></Routes>;return <Shell/>}
