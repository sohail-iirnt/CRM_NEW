import { useEffect, useMemo, useState } from 'react'
import { DISPATCH_METHODS, PDI_RESULTS, STATUSES, STATUS_LABELS } from '../config.js'
import { subscribeLogs, subscribeTickets, updateTicket } from '../services.js'
import { useAuth } from '../auth.jsx'
import TicketDetail, { TicketLink } from '../components/TicketDetail.jsx'

function Table({ tickets, action, onOpen }) { return <div className="table-wrap"><table><thead><tr><th>Ticket</th><th>Type</th><th>Customer</th><th>Destination</th><th>Qty</th><th>Status</th><th>Action</th></tr></thead><tbody>{tickets.map(ticket => <tr key={ticket.id}><td><TicketLink ticket={ticket} onOpen={onOpen} /></td><td><span className={`badge ${ticket.ticketType}`}>{ticket.ticketType}</span></td><td>{ticket.customerName}</td><td>{ticket.destinationBranch || ticket.destinationCompany || '—'}</td><td>{ticket.qty}</td><td>{STATUS_LABELS[ticket.currentStatus] || ticket.currentStatus}</td><td>{action ? action(ticket) : '—'}</td></tr>)}</tbody></table></div> }
function useData() { const [tickets, setTickets] = useState([]); const [logs, setLogs] = useState([]); useEffect(() => subscribeTickets(setTickets), []); useEffect(() => subscribeLogs(setLogs), []); return { tickets, logs } }
function History({ module, title, tickets, logs, onOpen }) { const ids = useMemo(() => new Set(logs.filter(log => log.module === module || log.role === module).map(log => log.ticketId)), [logs, module]); return <section className="page"><div className="eyebrow">{module.toUpperCase()}</div><h1>{title} History</h1><p className="muted">Tickets with immutable activity recorded by this department.</p><div className="card"><Table tickets={tickets.filter(ticket => ids.has(ticket.id))} onOpen={onOpen} /></div></section> }

export function Warehouse({ historyOnly = false }) { const { profile } = useAuth(); const { tickets, logs } = useData(); const [selected, setSelected] = useState(null); const [detail, setDetail] = useState(null); const [method, setMethod] = useState('Dedicated'); const [vehicle, setVehicle] = useState(''); const [courier, setCourier] = useState(''); const [tracking, setTracking] = useState(''); if (historyOnly) return <><History module="warehouse" title="Warehouse" tickets={tickets} logs={logs} onOpen={setDetail} /><TicketDetail ticket={detail} onClose={() => setDetail(null)} /></>; const pending = tickets.filter(ticket => ticket.currentStatus === STATUSES.PENDING_WAREHOUSE); const ready = tickets.filter(ticket => ticket.currentStatus === STATUSES.READY_FOR_DISPATCH); async function move(ticket) { const next = ticket.workflowSteps?.includes(STATUSES.READY_FOR_PDI) ? STATUSES.READY_FOR_PDI : STATUSES.READY_FOR_DISPATCH; await updateTicket(ticket.id, { currentStatus: next, currentModule: next === STATUSES.READY_FOR_PDI ? 'pdi' : 'warehouse', previousStatus: ticket.currentStatus }, profile, 'warehouse_movement_completed', { module: 'warehouse', previousStatus: ticket.currentStatus, newStatus: next, details: 'Warehouse movement completed.' }) } async function dispatch(event) { event.preventDefault(); if (method === 'Dedicated' && !vehicle) return; if (method === 'Courier' && (!courier || !tracking)) return; await updateTicket(selected.id, { currentStatus: STATUSES.LOGISTICS_PENDING, currentModule: 'logistics', previousStatus: selected.currentStatus, dispatch: { method, vehicleNumber: method === 'Dedicated' ? vehicle : '', courierName: method === 'Courier' ? courier : '', trackingId: method === 'Courier' ? tracking : '' } }, profile, 'warehouse_dispatch_completed', { module: 'warehouse', previousStatus: selected.currentStatus, newStatus: STATUSES.LOGISTICS_PENDING, details: `Dispatch completed by ${method}.`, metadata: { method, vehicle, courier, tracking } }); setSelected(null) } return <section className="page"><div className="eyebrow">WAREHOUSING</div><h1>Warehouse Operations</h1><div className="card"><h2>Pending Movement</h2><Table tickets={pending} onOpen={setDetail} action={ticket => <button className="primary" onClick={() => move(ticket)}>Mark Moved</button>} /></div><div className="card"><h2>Ready for Dispatch</h2><Table tickets={ready} onOpen={setDetail} action={ticket => <button className="primary" onClick={() => setSelected(ticket)}>Dispatch Done</button>} /></div>{selected && <div className="card"><h2>Dispatch Details — {selected.ticketId}</h2><form className="form-grid" onSubmit={dispatch}><label>Dispatch Method<select value={method} onChange={event => setMethod(event.target.value)}>{DISPATCH_METHODS.map(item => <option key={item}>{item}</option>)}</select></label>{method === 'Dedicated' ? <label>Vehicle Number<input value={vehicle} onChange={event => setVehicle(event.target.value)} required /></label> : <><label>Courier Name<input value={courier} onChange={event => setCourier(event.target.value)} required /></label><label>Tracking ID<input value={tracking} onChange={event => setTracking(event.target.value)} required /></label></>}<div className="form-actions"><button className="primary">Submit Dispatch</button><button type="button" className="secondary" onClick={() => setSelected(null)}>Cancel</button></div></form></div>}<TicketDetail ticket={detail} onClose={() => setDetail(null)} /></section> }

export function PDI({ historyOnly = false }) { const { profile } = useAuth(); const { tickets, logs } = useData(); const [selected, setSelected] = useState(null); const [detail, setDetail] = useState(null); const [result, setResult] = useState(PDI_RESULTS[0]); const [total, setTotal] = useState(''); const [damage, setDamage] = useState('0'); const [remarks, setRemarks] = useState(''); if (historyOnly) return <><History module="pdi" title="PDI" tickets={tickets} logs={logs} onOpen={setDetail} /><TicketDetail ticket={detail} onClose={() => setDetail(null)} /></>; const queue = tickets.filter(ticket => ticket.currentStatus === STATUSES.READY_FOR_PDI); async function submit(event) { event.preventDefault(); const checked = Number(total); const damaged = Math.min(checked, Number(damage)); await updateTicket(selected.id, { currentStatus: STATUSES.READY_FOR_DISPATCH, currentModule: 'warehouse', previousStatus: selected.currentStatus, pdi: { result, totalChecked: checked, defective: damaged, approved: checked - damaged, remarks } }, profile, 'pdi_inspection_completed', { module: 'pdi', previousStatus: selected.currentStatus, newStatus: STATUSES.READY_FOR_DISPATCH, details: `PDI inspection completed: ${result}.`, metadata: { result, checked, damaged, remarks } }); setSelected(null) } return <section className="page"><div className="eyebrow">PDI INSPECTION</div><h1>Inspection Queue</h1><div className="card"><Table tickets={queue} onOpen={setDetail} action={ticket => <button className="primary" onClick={() => { setSelected(ticket); setTotal(String(ticket.qty || '')); setDamage('0') }}>Inspect</button>} /></div>{selected && <div className="card"><h2>Inspection — {selected.ticketId}</h2><form className="form-grid" onSubmit={submit}><label>PDI Result<select value={result} onChange={event => setResult(event.target.value)}>{PDI_RESULTS.map(item => <option key={item}>{item}</option>)}</select></label><label>Total Checked<input type="number" min="0" value={total} onChange={event => setTotal(event.target.value)} required /></label><label>Defective / Damaged<input type="number" min="0" value={damage} onChange={event => setDamage(event.target.value)} required /></label><label>Detailed Remarks<textarea value={remarks} onChange={event => setRemarks(event.target.value)} rows="4" /></label><div className="form-actions"><button className="primary">Approve & Complete</button><button type="button" className="secondary" onClick={() => setSelected(null)}>Cancel</button></div></form></div>}<TicketDetail ticket={detail} onClose={() => setDetail(null)} /></section> }

export function Logistics({ historyOnly = false }) { const { profile } = useAuth(); const { tickets, logs } = useData(); const [eta, setEta] = useState(''); const [actual, setActual] = useState(''); const [selected, setSelected] = useState(null); const [detail, setDetail] = useState(null); if (historyOnly) return <><History module="logistics" title="Logistics" tickets={tickets} logs={logs} onOpen={setDetail} /><TicketDetail ticket={detail} onClose={() => setDetail(null)} /></>; const incoming = tickets.filter(ticket => ticket.currentStatus === STATUSES.LOGISTICS_PENDING); const transit = tickets.filter(ticket => ticket.currentStatus === STATUSES.IN_TRANSIT); async function submitEta(event) { event.preventDefault(); await updateTicket(selected.id, { currentStatus: STATUSES.IN_TRANSIT, currentModule: 'logistics', previousStatus: selected.currentStatus, logistics: { ...(selected.logistics || {}), etaDate: eta } }, profile, 'logistics_eta_submitted', { module: 'logistics', previousStatus: selected.currentStatus, newStatus: STATUSES.IN_TRANSIT, details: `ETA submitted: ${eta}.`, metadata: { etaDate: eta } }); setSelected(null) } async function close(event) { event.preventDefault(); await updateTicket(selected.id, { currentStatus: STATUSES.CLOSED, currentModule: 'closed', previousStatus: selected.currentStatus, logistics: { ...(selected.logistics || {}), actualBranchReportingDate: actual } }, profile, 'logistics_ticket_closed', { module: 'logistics', previousStatus: selected.currentStatus, newStatus: STATUSES.CLOSED, details: `Ticket closed; branch reporting date: ${actual}.`, metadata: { actualBranchReportingDate: actual } }); setSelected(null) } return <section className="page"><div className="eyebrow">LOGISTICS</div><h1>Logistics Operations</h1><div className="card"><h2>Logistics Queue</h2><Table tickets={incoming} onOpen={setDetail} action={ticket => <button className="primary" onClick={() => { setSelected(ticket); setEta('') }}>Add ETA & Submit</button>} /></div><div className="card"><h2>In Transit</h2><Table tickets={transit} onOpen={setDetail} action={ticket => <button className="primary" onClick={() => { setSelected(ticket); setActual('') }}>Close Ticket</button>} /></div>{selected && <div className="card"><h2>{selected.currentStatus === STATUSES.LOGISTICS_PENDING ? 'Submit ETA' : 'Close Ticket'} — {selected.ticketId}</h2><form className="form-grid" onSubmit={selected.currentStatus === STATUSES.LOGISTICS_PENDING ? submitEta : close}><label>{selected.currentStatus === STATUSES.LOGISTICS_PENDING ? 'ETA Date' : 'Branch Reporting Actual Date'}<input type="date" value={selected.currentStatus === STATUSES.LOGISTICS_PENDING ? eta : actual} onChange={event => selected.currentStatus === STATUSES.LOGISTICS_PENDING ? setEta(event.target.value) : setActual(event.target.value)} required /></label><div className="form-actions"><button className="primary">{selected.currentStatus === STATUSES.LOGISTICS_PENDING ? 'Submit Ticket' : 'Submit & Close Ticket'}</button><button type="button" className="secondary" onClick={() => setSelected(null)}>Cancel</button></div></form></div>}<TicketDetail ticket={detail} onClose={() => setDetail(null)} /></section> }
