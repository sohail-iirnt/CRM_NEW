import { useEffect, useState } from 'react'
import { subscribeLogs, subscribeTickets } from '../services.js'
import { DEFAULT_WORKFLOW_RULES } from '../workflow.js'

export default function Admin(){const[tickets,setTickets]=useState([]);const[logs,setLogs]=useState([]);useEffect(()=>subscribeTickets(setTickets),[]);useEffect(()=>subscribeLogs(setLogs),[]);const counts={total:tickets.length,inbound:tickets.filter(t=>t.ticketType==='inbound').length,outbound:tickets.filter(t=>t.ticketType==='outbound').length,transit:tickets.filter(t=>t.currentStatus==='in_transit').length,closed:tickets.filter(t=>t.currentStatus==='closed').length};return <section className="page"><div className="eyebrow">ADMIN CONTROL CENTER</div><h1>System Administration</h1><p className="muted">Central visibility for users, access, workflow rules, exports, settings and operational audit.</p><div className="kpi-grid">{Object.entries(counts).map(([k,v])=><div className="kpi" key={k}><small>{k}</small><strong>{v}</strong></div>)}</div><div className="card"><div className="card-head"><div><h2>Tickets Flow Manager</h2><p className="muted">These are the initial workflow rules. The next admin iteration will persist editable rules in Firestore.</p></div></div><div className="table-wrap"><table><thead><tr><th>Rule</th><th>Condition</th><th>Workflow</th><th>Enabled</th></tr></thead><tbody>{DEFAULT_WORKFLOW_RULES.map(r=><tr key={r.id}><td className="strong">{r.name}</td><td>{r.conditions.sources?.join(', ')||r.conditions.ticketType}{r.conditions.destinationCompany?` → ${r.conditions.destinationCompany}`:''}{r.conditions.destinationCompany==='__NOT_5000__'?' → not 5000':''}</td><td>{r.steps.join(' → ')}</td><td>{r.enabled?'Yes':'No'}</td></tr>)}</tbody></table></div></div><div className="card"><h2>Live System Logs</h2><div className="table-wrap"><table><thead><tr><th>Time</th><th>User</th><th>Role</th><th>Action</th><th>Ticket</th><th>Details</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{l.createdAt?.toDate?l.createdAt.toDate().toLocaleString():'—'}</td><td>{l.userName||'—'}</td><td>{l.role||'—'}</td><td>{l.action}</td><td>{l.ticketId||'—'}</td><td>{l.details||'—'}</td></tr>)}</tbody></table></div></div><div className="card"><h2>Administration Modules</h2><div className="admin-grid"><div><strong>Portal Settings</strong><p className="muted">System-level configuration and dropdown management.</p></div><div><strong>User Management</strong><p className="muted">Add, block, unblock and remove operational accounts.</p></div><div><strong>Access Control</strong><p className="muted">Role-based access for Admin, CRM, Warehouse, PDI and Logistics.</p></div><div><strong>Export Data</strong><p className="muted">CSV and PDF reporting layer will use the same live Firestore dataset.</p></div></div></div></section>}
